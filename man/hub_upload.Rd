% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/upload.R
\name{hub_upload}
\alias{hub_upload}
\title{Upload files to a Hugging Face repository}
\usage{
hub_upload(
  repo_id,
  path,
  path_in_repo = NULL,
  ...,
  repo_type = c("model", "dataset", "space"),
  revision = "main",
  commit_message = NULL,
  commit_description = "",
  token = hub_token()
)
}
\arguments{
\item{repo_id}{The repository identifier, e.g. \code{"username/repo"}.}

\item{path}{Path to a local file or directory to upload.}

\item{path_in_repo}{Destination path within the repository. For a
directory upload this is the destination folder (default: repo
root); for a single file it is the destination path (default: the
file's basename).}

\item{...}{Currently unused.}

\item{repo_type}{The repository type: \code{"model"} (default), \code{"dataset"}
or \code{"space"}.}

\item{revision}{The branch to commit to (default \code{"main"}).}

\item{commit_message}{Commit summary. A sensible default is generated.}

\item{commit_description}{Longer commit description (optional).}

\item{token}{A Hugging Face token with write access. Defaults to the
\code{HF_TOKEN} / \code{HUGGING_FACE_HUB_TOKEN} / \code{HUGGINGFACE_HUB_TOKEN}
environment variables.}
}
\value{
Invisibly, the list of repository paths that were committed.
}
\description{
Uploads a file or a whole directory to a repository on the Hugging Face
Hub over the HTTP(S) LFS protocol - no \code{git} or \code{git-lfs} binary
required. Large files (safetensors, bin, etc.) are stored with Git LFS
via presigned S3 uploads (single-part below 5 GB, multipart above);
small files ride inline in the commit.
}
\examples{
\dontrun{
hub_upload("username/model", "model.safetensors")
hub_upload("username/dataset", "local_dir", repo_type = "dataset")
}

}
